import 'package:flutter/material.dart';

class SetRatesScreen extends StatelessWidget {
  static const id = '/SetRatesScreen';
  const SetRatesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
